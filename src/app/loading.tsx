import Loader from '@/common/loader'
import PageWrapper from '@/common/page-wrapper'

const Loading = () => {
  return (
    <PageWrapper>
      <Loader />
    </PageWrapper>
  )
}

export default Loading
